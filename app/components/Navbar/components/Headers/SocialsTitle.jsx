import { motion as m } from "framer-motion"
import { centerLinkVars } from "../../helpers/centerLinkVars"

export const SocialsTitle = () => {
    return(
     <m.h3 className="text-2xl font-bold" variants={centerLinkVars}>Obserwuj nas:</m.h3>
    )
  }